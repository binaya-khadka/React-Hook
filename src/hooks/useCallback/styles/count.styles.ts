import { pageContainer } from "../../../page-reset";

const container = {
  ...pageContainer,
  display: 'grid',
  placeItemsCenter: 'center',
  justifyContent: 'center',
  alignItems: 'center',
  margin: '2rem auto'
}


export { container }